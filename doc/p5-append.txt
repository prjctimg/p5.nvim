append()

Adds a value to the end of an array. Extends the
         *   length of the array by one. Maps to Array.push().

Parameters:
  array: Array to append
         *   @param value to be added to the Array

Returns: the array that was appended to
