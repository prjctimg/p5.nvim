*p5-typography*                                         p5.js typography docs

==============================================================================
Table of Contents                           *p5-typography-table-of-contents*

1. Functions                                                |p5-typography-functions|
  - textAlign()              |p5-typography-textAlign|
  - textLeading()              |p5-typography-textLeading|
  - textSize()              |p5-typography-textSize|
  - textStyle()              |p5-typography-textStyle|
  - textWidth()              |p5-typography-textWidth|
  - textAscent()              |p5-typography-textAscent|
  - textDescent()              |p5-typography-textDescent|
  - textWrap()              |p5-typography-textWrap|
  - text()              |p5-typography-text|
  - textFont()              |p5-typography-textFont|
  - constructor()              |p5-typography-constructor|
  - textBounds()              |p5-typography-textBounds|
  - textToPoints()              |p5-typography-textToPoints|

📝 Typography module for p5.js

📦 p5.js Version: 1.7.7~
⏰ Last Updated: 2026-03-16 04:28:14Z~

==============================================================================
2. Functions                                                |p5-typography-functions|


`textAlign()`                                            |p5-typography-textAlign|

Sets the current alignment for drawing text. Accepts two arguments: horizAlign
(LEFT, CENTER, or RIGHT) and vertAlign (TOP, BOTTOM, CENTER, or BASELINE). The
horizAlign parameter is in reference to the x value of the text() function,
while the vertAlign parameter is in reference to the y value. So if you write
textAlign(LEFT), you are aligning the left edge of your text to the x value
you give in text(). If you write textAlign(RIGHT, TOP), you are aligning the
right edge of your text to the x value and the top edge of the text to the y
value.

#### Parameters:

    horizAlign  horizontal alignment, either LEFT, CENTER, or RIGHT

#### Returns:

    void

📂 *Sub-module: attributes*

---


`textLeading()`                                            |p5-typography-textLeading|

Sets/gets the spacing, in pixels, between lines of text. This setting will be
used in all subsequent calls to the text() function.

#### Returns:

    void

📂 *Sub-module: attributes*

---


`textSize()`                                            |p5-typography-textSize|

Sets/gets the current font size. This size will be used in all subsequent
calls to the text() function. Font size is measured in pixels.

#### Returns:

    void

📂 *Sub-module: attributes*

---


`textStyle()`                                            |p5-typography-textStyle|

Sets/gets the style of the text for system fonts to NORMAL, ITALIC, BOLD or
BOLDITALIC. Note: this may be is overridden by CSS styling. For non-system
fonts (opentype, truetype, etc.) please load styled fonts instead.

#### Returns:

    void

📂 *Sub-module: attributes*

---


`textWidth()`                                            |p5-typography-textWidth|

Calculates and returns the width of any character or text string.

#### Parameters:

    theText  the String of characters to measure

#### Returns:

    void

📂 *Sub-module: attributes*

---


`textAscent()`                                            |p5-typography-textAscent|

Returns the ascent of the current font at its current size. The ascent
represents the distance, in pixels, of the tallest character above the
baseline.

#### Returns:

    void

📂 *Sub-module: attributes*

---


`textDescent()`                                            |p5-typography-textDescent|

Returns the descent of the current font at its current size. The descent
represents the distance, in pixels, of the character with the longest
descender below the baseline.

#### Returns:

    void

📂 *Sub-module: attributes*

---


`textWrap()`                                            |p5-typography-textWrap|

Specifies how lines of text are wrapped within a text box. This requires a
max-width set on the text area, specified in text() as parameter x2. WORD wrap
style only breaks lines at spaces. A single string without spaces that exceeds
the boundaries of the canvas or text area is not truncated, and will overflow
the desired area, disappearing at the canvas edge. CHAR wrap style breaks
lines wherever needed to stay within the text box. WORD is the default wrap
style, and both styles will still break lines at any line breaks (\n)
specified in the original text. The text area max-height parameter (y2) also
still applies to wrapped text in both styles, lines of text that do not fit
within the text area will not be drawn to the screen.

#### Parameters:

    wrapStyle  text wrapping style, either WORD or CHAR

#### Returns:

    void

📂 *Sub-module: attributes*

---


`text()`                                            |p5-typography-text|

Loads an opentype font file (.otf, .ttf) from a file or a URL, and returns a
p5.Font object. This function is asynchronous, meaning it may not finish
before the next line in your sketch is executed. The path to the font should
be relative to the HTML file that links in your sketch. Loading fonts from a
URL or other remote location may be blocked due to your browser's built-in
security.

#### Parameters:

    path  name of the file or url to load
    str  the alphanumeric symbols to be displayed
    x  x-coordinate of text
    y  y-coordinate of text

#### Returns:

    p5.Font object

📂 *Sub-module: loading_displaying*

---


`textFont()`                                            |p5-typography-textFont|

Sets the current font that will be drawn with the text() function. If
textFont() is called without any argument, it will return the current font if
one has been set already. If not, it will return the name of the default font
as a string. If textFont() is called with a font to use, it will return the p5
object. WEBGL: Only fonts loaded via loadFont() are supported.

#### Returns:

    void

📂 *Sub-module: loading_displaying*

---


`constructor()`                                            |p5-typography-constructor|

Base class for font handling

#### Returns:

    void

📂 *Sub-module: p5.Font*

---


`textBounds()`                                            |p5-typography-textBounds|

Returns a tight bounding box for the given text string using this font

#### Parameters:

    line  a line of text
    x  x-position
    y  y-position

#### Returns:

    void

📂 *Sub-module: p5.Font*

---


`textToPoints()`                                            |p5-typography-textToPoints|

Computes an array of points following the path for specified text

#### Parameters:

    txt  a line of text
    x  x-position
    y  y-position
    fontSize  font size to use (optional)

#### Returns:

    void

📂 *Sub-module: p5.Font*

---

==============================================================================
Autogenerated by automata <https://github.com/prjctimg/automata>

vim:tw=78:ts=8:ft=help:norl:
