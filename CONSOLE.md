The lua/console.lua file has a few adjustments needed

The keymaps for the console should work for both terminal and normal mode. Arrow keys and the default Vim navigation keys should scroll up/down the buffer.
Ensure that creating a console window doesn't close the currently opened file.
